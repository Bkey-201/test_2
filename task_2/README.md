## Task 2: Обработка COCO-датасета

Скрипты для реструктуризации, валидации и преобразования COCO-датасета.  
Входной файл аннотаций – **`instances_train.json`** (по умолчанию).

### Скрипты
- `restructure_coco.py` – реструктуризация папок по классам: перемещает изображения в папки `images/<класс>` или `images/<класс1_класс2>`, обновляет `file_name` в JSON.
- `validate_coco.py` – валидация обновлённого датасета: проверяет существование файлов, целостность ссылок (`image_id`, `category_id`), формирует отчёт в JSON.
- `convert_to_yolo.py`  – преобразование аннотаций в формат YOLO: создаёт `.txt` файлы с нормализованными координатами для каждого изображения.

### Структура папки
